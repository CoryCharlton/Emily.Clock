We use .NET nanoFramework.

For unit tests we use nanoFramework.TestFramework.

We do not use obsolete Assert methods for unit tests (eg: we use Assert.AreEqual over Assert.Equal).

We do not comment unit tests with unecessary statements such as "arrange", "act", or "assert".